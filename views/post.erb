<article>
    <p class="dingbats">~<span class="star">✧</span>~</p>
    <h1>
      <a href="<%= url(post.slug) %>"><%= post.title %></a>
    </h1>
    <time class="timeago" datetime="<%= Time.parse(post.date.to_s).xmlschema %>">
      <%= Time.parse(post.date.to_s).strftime "%Y/%m/%d" %>
    </time>
  <section class="content">
    <%= markdown post.content %>
  </section>
</article>
